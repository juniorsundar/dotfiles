[user]
	email = junior.sundar@tii.ae
	name = Junior Sundar
[credential]
	helper = store
[gui]
	recentrepo = /home/juniorsundar/Dropbox/neorg
[core]
    pager = delta --dark --paging=always --line-numbers --hyperlinks
    editor = nvim
[interactive]
    diffFilter = delta --color-only
[delta]
    line-numbers = true
    side-by-side = false
    dark = true
    features = colibri
    navigate = true
[pager]
    blame  = delta --dark --paging=never --line-numbers --hyperlinks
    diff   = delta --dark --paging=never --line-numbers --hyperlinks
    reflog = delta --dark --paging=never --line-numbers --hyperlinks
    show   = delta --dark --paging=never --line-numbers --hyperlinks
[merge]
    conflictstyle = zdiff3
    tool = nvimdiff
[diff]
    colorMoved = default
[safe]
	directory = /etc/nixos
